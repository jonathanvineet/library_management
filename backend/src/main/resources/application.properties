# Server Configuration
server.port=8080

# Database Configuration (Direct connection with reduced pool size for Supabase free tier)
spring.datasource.url=jdbc:postgresql://db.hnpgjwbhndackowriqau.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=tkt@vav@vj@u1
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Connection Pool Settings (Reduced for Supabase free tier)
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Admin Account Configuration (Set via environment variables for security)
admin.email=${ADMIN_EMAIL:admin@gmail.com}
admin.password=${ADMIN_PASSWORD:admin123}
admin.name=${ADMIN_NAME:Admin}
