import { createlmsConfig } from "lms-agent-playwright-config/config";

export default createlmsConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
